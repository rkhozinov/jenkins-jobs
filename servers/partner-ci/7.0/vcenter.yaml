# jobs for test-group
- defaults:
    name: global
    logrotate:
        daysToKeep: 7
        numToKeep: 10

    properties:
      - heavy-job:
          weight: 1

    parameters:
      - string:
          name: ISO_FILE
          default: ''
          description: "The fuel iso name, downloded by the 'download_iso' job to tpi-s1 storage"
      - bool:
          name: USE_SNAPSHOTS
          default: true
          descrition: 'Will be used existing environment'
      - bool:
          name: SHUTDOWN_AFTER
          default: true
          decsription: 'The env will be powered off after test'
      - bool:
          name: ERASE_AFTER
          default: false
          description: 'The env will be erased after test'
    wrappers:
      - ansicolor:
          colormap: xterm
      - timeout:
          fail: true
          timeout: 6000
- project:
   name: '7.0.vcenter.systest'
   description: '7.0 vcenter system tests'
   fuel-release: '7.0'
   id: 'systest'
   repobranch: 'origin/stable/7.0'
   reponame: 'fuel-qa'
   repourl: 'https://github.com/stackforge/{reponame}.git'
   venvpath: '/home/jenkins/venv-nailgun-tests-2.9'
   runner-name: '{fuel-release}.{id}.vcenter.runner'
   runner-properties: 'properties'
   mail-to: 'rkhozinov@mirantis.com'
   default_pool: '10.109.0.0/16:24'
   nodes-count: '9'
   openstack-release: 'Ubuntu'
   vcenter-snapshot: 'vcenterha'
   iso-storage: '/storage/downloads'
   env-prefix: '{fuel-release}.vcenter'
   testgroup:
       - vcenter_smoke
       - vcenter_bvt
       - vcenter_cindervmdk
       - vcenter_computevmware
       - vcenter_cindervmdk_and_computevmware
       - vcenter_glance_backend
       - vcenter_multiple_cluster_with_computevmware
       - vcenter_ceph
       - vcenter_computevmware_and_ceph
       - vcenter_multiroles_cindervmdk_and_ceph
       - vcenter_multiroles_cindervmdk_and_cinder
       - vcenter_ceilometer
       - vcenter_ceilometer_and_computevmware
       - vcenter_multiroles_ceilometer
       - vcenter_add_delete_nodes
       - vcenter_delete_controler
       - vcenter_ha_ceph
       - vcenter_ha_cinder_and_ceph
       - vcenter_ha_glance_backend_multiple_cluster
       - vcenter_ha_multiroles_cindervmdk_and_ceph
       - vcenter_ha_multiroles_cindervmdk_and_cinder
       - vcenter_ha_nova_flat_multiple_clusters
       - vcenter_ha_nova_vlan_multiple_clusters
   jobs:
     - '{runner-name}'
     - '{fuel-release}.{id}.{testgroup}'

## Jobs for system tests by testgroup
- job-template:
    name: '{fuel-release}.{id}.{testgroup}'
    concurrent: false
    node: 'vsphere'
    properties:
      - throttle:
          max-per-node: 0
          max-total: 0
          categories:
            - vsphere
          option: category
    scm:
      - git:
          url: '{repourl}'
          branches:
            - '{repobranch}'
    builders:
      - inject:
          properties-content: |
              NODES_COUNT={nodes-count}
              VENV_PATH={venvpath}
              FUEL_RELEASE={fuel-release}
              OPENSTACK_RELEASE={openstack-release}
              ENV_PREFIX={env-prefix}
              ENV_NAME=''
              VCENTER_SNAPSHOT={vcenter-snapshot}
              ISO_STORAGE={iso-storage}
              TEST_GROUP={testgroup}
      - shell:
          !include-raw-escape builders/systest.vcenter.prepare.sh
      - shell:
          !include-raw-escape builders/systest.vcenter.sh
    publishers:
      - description-setter:
          description: '$ISO_VERSION'
          description-for-failed: '$ISO_VERSION'
      - xunit:
          types:
            - junit:
                pattern: '**/nosetest.xml'
                skip-if-no-test-files: true

# multijob configuration
# for each of testgroups a job was created early in this script.
# and this job is needed to create the one run point for the all created jobs
- job-template:
    project-type: multijob
    name: '{runner-name}'
    description: "System tests for vcenter feature"
    node: runner
    triggers:
        - timed: 'H 19  * * *'
    builders:
      - copyartifact:
          project: 'download_iso'
          filter: 'latest_isos.txt'
          which-build: 'last-successful'
      - inject:
          properties-file: latest_isos.txt
      - inject:
          properties-content: |
            properties_file={runner-properties}
      - shell:
          !include-raw-escape builders/systest.vcenter.runner.sh
      - multijob:
          name: 'System tests for vcenter'
          condition: COMPLETED
          projects:
            - name: '7.0.systest.vcenter_bvt'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_cindervmdk'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_computevmware'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_cindervmdk_and_computevmware'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_glance_backend'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_multiple_cluster_with_computevmware'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_computevmware_and_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_multiroles_cindervmdk_and_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_multiroles_cindervmdk_and_cinder'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ceilometer'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ceilometer_and_computevmware'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_multiroles_ceilometer'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_add_delete_nodes'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_delete_controler'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_cinder_and_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_glance_backend_multiple_cluster'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_multiroles_cindervmdk_and_ceph'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_multiroles_cindervmdk_and_cinder'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_nova_flat_multiple_clusters'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

            - name: '7.0.systest.vcenter_ha_nova_vlan_multiple_clusters'
              current-parameters: true
              property-file: '{runner-properties}'
              kill-phase-on: NEVER

    publishers:
        - archive:
            artifacts: '**/nosetests.xml,logs/*'
            allow-empty: true
            fingerprint: true
        - xunit:
            types:
              - junit:
                  pattern: '**/nosetest.xml'
                  skip-if-no-test-files: true
        - email:
            recipients: '{mail-to}'
