- defaults:
    name: global
    disabled: false
    logrotate:
      daysToKeep: 7         
      numToKeep: 10
      artifactDaysToKeep: 7
      artifactNumToKeep: 10
    properties:
      - heavy-job:
          weight: 1           

- project:
    fuel-release: 6.1
    plugin-version: 1.0-1
    plugin-name: dvs 
    name: '{fuel-release}.{plugin-name}.{plugin-version}.project'
    build-name: '{fuel-release}.{plugin-name}.{plugin-version}.build'
    bvt-name: '{fuel-release}.{plugin-name}.{plugin-version}.bvt'
    iso-file: fuel-6.1-525-2015-06-19_13-02-31.iso
    gerrit-repo: fuel-plugin-vmware-dvs
    gerrit-branch: stable/6.1
    email-to: rkhozinov@mirantis.com #List of emails separated by comma

    nodes-count: '9'
    admin-node-memory: 4096
    slave-node-memory: 4096
    admin-node-cpu: 4
    slave-node-cpu: 4
    openstack-release: 'Ubuntu'
    vcenter-snapshot: 'vcenterha'
    iso-storage: '/storage/downloads'

    testgroup:
      - dvs_vcenter_bvt
      - dvs_vcenter_smoke
      - dvs_vcenter_ha_mode
      - dvs_vcenter_ceph
      - dvs_vcenter_ceilometer
      - dvs_vcenter_add_delete_nodes
      - dvs_vcenter_plugin
      - dvs_vcenter_multiroles
      - dvs_vcenter_add_delete_controller
      - dvs_vcenter_5_instances
      - dvs_vcenter_networks
      - dvs_vcenter_reset_controller
      - dvs_vcenter_diff_networks
      - dvs_vcenter_multiple_nics
      - dvs_vcenter_uninstall
      - dvs_vcenter_tenants_isolation
      - dvs_vcenter_system
      - dvs_vcenter_same_ip
      - dvs_vcenter_security
    jobs:
      - {build-name}

- job-template:
    name: '{build-name}'
    node: 'runner'
    builders:
      - shell:
          !include-raw-escape './builders/build-plugin.sh'
    description: |
        '<a href=https://github.com/stackforge/{gerrit-repo}>
        Build {name} plugin from fuel-plugins project</a>'
    parameters:
      - string:
          name: 'GERRIT_REFSPEC'
          default: 'refs/heads/{gerrit-branch}'
    scm:
      - git:
          branches:
            - $GERRIT_BRANCH
          name: ''
          refspec: $GERRIT_REFSPEC
          url: 'https://review.openstack.org/stackforge/{gerrit-repo}'
          choosing-strategy: gerrit
          clean:
            before: true
    triggers:
      - gerrit:
          trigger-on:
            - patchset-created-event #Trigger plugin build for every gerrit patchset
          projects:
            - project-compare-type: 'PLAIN'
              project-pattern: '{gerrit-repo}'
              branches:
                - branch-compare-type: 'ANT'
                  branch-pattern: '**'
          silent: true
          server-name: 'review.openstack.org'
    publishers:
      - archive:
          artifacts: '*.rpm'
      - email:
          notify-every-unstable-build: true
          recipients: '{email_to}'

- job-template:
    name: '{fuel-release}.{plugin-name}.{plugin-version}.{testgroup}'
    description: 'The test "{testgroup}" for {fuel-release}.{plugin-name}.{plugin-version}'
    parameters:
      #TODO: add env prefix
      #TODO: USE_SNAPSHOTS
      #TODO: SHUTDOWN_AFTER
      #TODO: ERASE_AFTER
      - string:
          name: ISO_FILE
          default: {iso-file} 
          description: 'ISO file name that is on the tpi-s1 in /storage/downloads'
      - string:
          name: GERRIT_REFSPEC
          default: 'refs/heads/{gerrit-branch}'
          description: 'Refspecs for commits in fuel-qa gerrit separated with spaces'
      - string:
          name: GERRIT_BRANCH
          default: 'origin/{gerrit-branch}'
          description: 'The branch for fuel-qa gerrit'

    builders:
      - copyartifact:
          project: {build-name} 
          which-build: last-successful
      - inject:
          properties-content: |
              NODES_COUNT={nodes-count}
              ADMIN_NODE_MEMORY={admin-node-memory}
              SLAVE_NODE_MEMORY={slave-node-memory}
              ADMIN_NODE_CPU={admin-node-cpu} 
              SLAVE_NODE_CPU={slave-node-cpu}
              VENV_PATH={venvpath}
              FUEL_RELEASE={fuel-release}
              OPENSTACK_RELEASE={openstack-release}
              ENV_PREFIX={env-prefix}
              VCENTER_SNAPSHOT={vcenter-snapshot}
              ISO_STORAGE={iso-storage}
              TEST_GROUP={testgroup}
              ENV_NAME=''
      - shell:
          !include-raw-escape builders/dvs.prepare.sh
      - shell:
          !include-raw-escape builders/dvs.systest.sh
    properties:
      - throttle:
          max-per-node: 0
          max-total: 0
          categories:
            - vsphere      
            - dvs          
            - nsx
          option: category 
    scm:
      - git:
          branches:
            - $GERRIT_BRANCH
          refspec: $GERRIT_REFSPEC
          url: 'https://review.openstack.org/stackforge/fuel-qa'
          choosing-strategy: gerrit
          clean:
            before: true
          wipe-workspace: false
            
    #TODO: add wrappers        
    publishers:
      #TODO: add description-setter
      - archive:
          artifacts: 'logs/$BUILD_NUMBER/*'
      - email:
          notify-every-unstable-build: true
          recipients: '{email_to}'

- job-template:
    name: '{name}.custom'
    builders:
      - copyartifact:
          project: '{name}.build'
          which-build: last-successful
      - inject:
          properties-content: |
            OPENSTACK_RELEASE=ubuntu
            TEST_GROUP={test_group}
            ISO_PATH={path_to_fuel_iso}
      - shell:
          !include-raw-escape './builders/prepare_env.sh'
      - shell:
          !include-raw-escape './builders/deploy-plugin.sh'
    description: 'fuel-qa system test for {name}'
    logrotate:
      numToKeep: 10
    parameters:
      - string:
          name: 'GERRIT_REFSPEC'
          default: 'refs/heads/master'
    scm:
      - git:
          branches:
            - $GERRIT_BRANCH
          refspec: $GERRIT_REFSPEC
          url: 'https://review.openstack.org/stackforge/fuel-qa'
          choosing-strategy: gerrit
          clean:
            before: true
          wipe-workspace: false
    publishers:
      - archive:
          artifacts: 'logs/$BUILD_NUMBER/*'
      - email:
          notify-every-unstable-build: true
          recipients: '{email_to}'

- job-template:
    name: '{name}.nightly'
    builders:
      - copyartifact:
          project: '{name}.build'
          which-build: last-successful
      - inject:
          properties-content: |
            OPENSTACK_RELEASE=ubuntu
            TEST_GROUP={test_group}
            ISO_PATH={path_to_fuel_iso}
      - shell:
          !include-raw-escape './builders/prepare_env.sh'
      - shell:
          !include-raw-escape './builders/deploy-plugin.sh'
    description: 'fuel-qa system test for {name}'
    logrotate:
      numToKeep: 10
    parameters:
      - string:
          name: 'GERRIT_REFSPEC'
          default: 'refs/heads/master'
    scm:
      - git:
          branches:
            - $GERRIT_BRANCH
          refspec: $GERRIT_REFSPEC
          url: 'https://review.openstack.org/stackforge/fuel-qa'
          choosing-strategy: gerrit
          clean:
            before: true
          wipe-workspace: false
    publishers:
      - archive:
          artifacts: 'logs/$BUILD_NUMBER/*'
      - email:
          notify-every-unstable-build: true
          recipients: '{email_to}'
